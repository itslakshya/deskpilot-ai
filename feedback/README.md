Active-learning feedback lands here at runtime (`queue.csv`, created automatically by
`backend/app.py`'s `/feedback` endpoint). Empty in the repo by design - this directory
is where corrected low-confidence predictions accumulate between retraining runs.
